---
import SiteLayout from "../layouts/SiteLayout.astro";
import ThemePanel from "../components/ThemePanel.astro";
---

<SiteLayout
  title="Alan Palayil — Application Development Analyst (FinTech/Data) | Cybersecurity"
  description="Portfolio of Alan Palayil. Data engineering, FinTech systems, Azure Synapse, and cybersecurity-minded automation."
>
  <section class="hero">
    <div>
      <h1>Building reliable FinTech automation & data pipelines.</h1>
      <p class="sub">
        Application Development Analyst (Genworth) — Azure Synapse, Python, SQL, and production support.
        I blend data engineering + security thinking to ship resilient systems.
      </p>

      <div class="cta">
        <a class="btn primary" href="/resume">View Resume</a>
        <a class="btn" href="/projects">Projects</a>
        <a class="btn" href="/contact">Contact</a>
      </div>

      <div class="chips">
        <span>Azure Synapse</span><span>Python</span><span>SQL</span><span>Spark</span><span>ETL</span><span>Security</span>
      </div>
    </div>

    <div class="panel">
      <ThemePanel />
    </div>
  </section>

  <section class="grid">
    <article class="card">
      <h2>Featured</h2>
      <p class="muted">Pin 2–3 best projects here with a one-line impact statement.</p>
      <a class="link" href="/projects">Go to Projects →</a>
    </article>

    <article class="card">
      <h2>Impact</h2>
      <ul>
        <li>Built and supported production data + message generation pipelines.</li>
        <li>Improved reliability through validations, automation, and failure handling.</li>
        <li>Experience across financial transaction data and operational workflows.</li>
      </ul>
    </article>

    <article class="card">
      <h2>Now</h2>
      <p class="muted">Add 1–2 lines on what you’re currently doing / looking for.</p>
      <p>Open to roles in Data Engineering / FinTech Platform / Security-minded Automation.</p>
    </article>
  </section>

  <style>
    h1 { font-size: 44px; line-height: 1.05; margin: 0 0 12px; letter-spacing: -0.6px; }
    .sub { color: var(--muted); font-size: 16px; line-height: 1.6; margin: 0 0 18px; max-width: 62ch; }
    .hero { display:grid; grid-template-columns: 1.3fr 0.7fr; gap: 18px; align-items: start; }
    .panel { border: 1px solid var(--border); background: rgba(255,255,255,0.02); border-radius: 16px; padding: 14px; }

    .cta { display:flex; gap: 10px; flex-wrap: wrap; margin-bottom: 14px; }
    .btn { border: 1px solid var(--border); background: rgba(255,255,255,0.02); padding: 10px 14px; border-radius: 999px; color: var(--text); }
    .btn.primary { border-color: rgba(138,180,255,0.45); background: rgba(138,180,255,0.12); }

    .chips { display:flex; gap: 8px; flex-wrap: wrap; margin-top: 6px; }
    .chips span { border: 1px solid var(--border); color: var(--muted); padding: 6px 10px; border-radius: 999px; font-size: 13px; }

    .grid { display:grid; grid-template-columns: repeat(3, 1fr); gap: 14px; margin-top: 18px; }
    .card { border: 1px solid var(--border); background: rgba(255,255,255,0.02); border-radius: 16px; padding: 14px; }
    .card h2 { margin: 0 0 8px; font-size: 16px; }
    .muted { color: var(--muted); }
    .link { display:inline-block; margin-top: 10px; color: var(--accent); }

    ul { margin: 10px 0 0; padding-left: 18px; color: var(--muted); }
    li { margin: 6px 0; }

    @media (max-width: 900px) {
      .hero { grid-template-columns: 1fr; }
      h1 { font-size: 36px; }
      .grid { grid-template-columns: 1fr; }
    }
  </style>
</SiteLayout>
